const initialState= {
    name: '',
    description: '',
    days: []
}

function itineraryReducer (state = initialState, action) {
    switch (action.type) {
        default:
            return state
    }
}

export default itineraryReducer